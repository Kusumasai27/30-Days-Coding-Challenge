class Solution:
    def lengthOfLastWord(self, s: str) -> int:
        x="1" 
        y=[]
        for i in reversed (s):
            if i>x:
                y.append(i)
            if len(y)>0:
                 if i<x:
                   break
        return len(y)
        
